#ifndef AMATH583_H
#define AMATH583_H

#include <iostream>
#include <cstring>
#include <fstream>
#include <vector>
#include <cstdlib>

std::vector<double> readVector(std::istream &inputStream);

std::vector<double> randomVector(int n);

#endif
